package petShop;

public class Cat {   // Creates the class Cat
    private int catSpaceNumber;

    public Cat(){   // Creates the constructor for the Cat class
        catSpaceNumber = 0;
    }

    public int getCatSpaceNumber() {   // Creates the call function for catSpace number
        return catSpaceNumber;
    }

    public void setCatSpaceNumber(int catSpaceNumber) {   // Creates the set function for catSpace number
        this.catSpaceNumber = catSpaceNumber;
    }
}
